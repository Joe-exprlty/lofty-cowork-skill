// Stale scratch file from a prior session. Safe to delete; not loaded
// by anything. Kept as a one-line stub so future packagings don't ship
// the 36KB worker copy that lived here. Run `rm` on this path to remove
// it entirely, or leave it as a no-op marker.
